package main

import ("fmt"
	"os"
	"bufio"
	"unicode"
)
func main() {
	var text, operator string

	scanner := bufio.NewReader(os.Stdin)

	text, _ := bufio.NewReader(os.Stdin)

	rs := rune[](text)


	if unicode.IsLetter(rs[i])


}